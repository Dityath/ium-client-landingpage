import React from "react";

const SubTitle = ({ children }) => {
  return (
    <h2 className="uppercase text-qolbiDark text-xl mx-auto font-bold text-center">
      {children}
    </h2>
  );
};

export default SubTitle;
